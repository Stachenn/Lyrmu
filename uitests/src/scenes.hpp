#pragma once

#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <SDL2/SDL_ttf.h>

void bar(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);
void search(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);
void settings(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);
void library(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);
void home(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);
void random(SDL_Window* window, SDL_Renderer* renderer, SDL_Color backgroundColor);

